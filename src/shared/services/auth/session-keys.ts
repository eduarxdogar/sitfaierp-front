/**
 * Claves usadas en sessionStorage para controlar el flujo de autenticación.
 */

/** Fuerza un login explícito en el próximo bootstrap (evita SSO silencioso tras logout). */
export const FORCE_LOGIN_STORAGE_KEY = "force_login";
